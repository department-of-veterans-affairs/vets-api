<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      body {
        font-family: sans-serif;
        font-size: smaller;
      }
      table {
        border-collapse: collapse;
      }
      table tr th, table tr td {
        border: 1px solid #d3d3d3;
        padding: 10px;
      }
      table tr th {
        padding: 5px 10px;
        white-space: nowrap;
        color: #808080;
        font-size: smaller;
        text-transform: uppercase;
        font-weight: normal;
        text-align: left;
      }
      table tr td {
        padding: 5px 10px;
        vertical-align: top;
      }
      table tr td.right-align {
        text-align: right;
      }
      h2 {
        margin: 30px 0 2px;
      }
      h2 span.title {
        color: #808080;
      }
    </style>
  </head>

  <body>
    <h1><%= @friendly_duration %> <%= @friendly_env %> Decision Review Report from <%= @report.from %> -- <%= @report.to %> </h1>

    <%= render partial: 'appeal_table', locals: { appeal_name: 'HLR', appeal_status_and_count: @report.hlr_by_status_and_count, faulty_appeals: @report.faulty_hlr, total_count: @friendly_duration == 'Weekly' ? @report.total_hlr_successes : nil } %>

    <br />
    <hr />

    <%= render partial: 'appeal_table', locals: { appeal_name: 'NOD', appeal_status_and_count: @report.nod_by_status_and_count, faulty_appeals: @report.faulty_nod, total_count: @friendly_duration == 'Weekly' ? @report.total_nod_successes : nil } %>

    <br />
    <hr />

    <%= render partial: 'appeal_table', locals: { appeal_name: 'NOD Evidence', appeal_status_and_count: @report.evidence_submission_by_status_and_count, faulty_appeals: @report.faulty_evidence_submission, total_count: nil } %>

    <br />
    <hr />

    <%= render partial: 'appeal_table', locals: { appeal_name: 'SC', appeal_status_and_count: @report.sc_by_status_and_count, faulty_appeals: @report.faulty_sc, total_count: @friendly_duration == 'Weekly' ? @report.total_sc_successes : nil } %>

    <br />
    <hr />

    <%= render partial: 'appeal_table', locals: { appeal_name: 'SC Evidence', appeal_status_and_count: @report.sc_evidence_submission_by_status_and_count, faulty_appeals: @report.sc_faulty_evidence_submission, total_count: nil } %>
  </body>
</html>
