{{- $root := . -}}
{{- if gt .Capabilities.KubeVersion.Minor "26" }}
# NEW EKS CLUSTER
---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vets-api-secretstore
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-4"
spec:
  provider:
    aws:
      service: ParameterStore
      region: us-gov-west-1
      auth:
        jwt:
          serviceAccountRef:
            name: vets-api

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: common-environment-vars
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-3"
spec:
  secretStoreRef:
    name: vets-api-secretstore
    kind: SecretStore
  data:
  {{- range $key, $value := $root.Values.parameters }}
    - secretKey: {{ $key }}
      remoteRef:
        key: {{ $root.Values.ssmPath }}{{ $value }}
  {{- end }}

{{- else }}
##########################################
# OLD EKS CLUSTER
---
apiVersion: "kubernetes-client.io/v1"
kind: ExternalSecret
metadata:
  name: common-environment-vars
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-3"
spec:
  backendType: systemManager
  data:
  {{- range $varName, $relPath := $root.Values.parameters }}
    - key: {{ $root.Values.ssmPath }}{{ $relPath }}
      name: {{ $varName }}
  {{- end }}

{{- end }}