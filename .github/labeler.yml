# Add mobile label to any change within the modules/mobile directory
mobile:
  - any: ['modules/mobile/**/*']
  
# Add vaos label to any change within the modules/vaos directory
vaos:
  - any: ['modules/vaos/**/*']

# Add be-review label to any change not within the modules directory
be-review:
  - any: ['./**/*', '!modules/**/*']
