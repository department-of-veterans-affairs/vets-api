type: array
items:
  type: object
  required:
    - preferenceId
    - preferenceName
    - value
  properties:
    preferenceId:
      type: string
      example: "appointment_reminders"
    preferenceName:
      type: string
      example: "Appointment Reminders"
    value:
      type: boolean
      example: true
