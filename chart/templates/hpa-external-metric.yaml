{{- if .Values.web.pumaBacklog.enabled }}
---
apiVersion: datadoghq.com/v1alpha1
kind: DatadogMetric
metadata:
  name: puma-backlog
  namespace: {{ .Release.Namespace }}
spec:
  query: {{ .Values.web.autoscaling.scaling_metric }}
{{- end }}
