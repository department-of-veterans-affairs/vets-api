{{- if .Values.web.ssoeEnabled }}
{{- range $keys, $key := .Values.web.ssoeConfigs }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $key.name }}
data:
{{ $key.key | indent 2  }}:
{{ $key.value | indent 4 }}
{{- end }}
{{- end }}
