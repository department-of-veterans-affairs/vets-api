---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vets-api-ssm-secretstore
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: "-4"
spec:
  provider:
    aws:
      service: ParameterStore
      region: us-gov-west-1
      auth:
        jwt:
          serviceAccountRef:
            name: vets-api
