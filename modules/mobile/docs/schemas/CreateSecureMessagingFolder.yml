type: object
additionalProperties: false
required:
  - folder
properties:
  folder:
    type: object
    additionalProperties: false
    required:
      - name
    properties:
      name:
        type: string
        example: "New Folder"
