type: object
required:
  - data
  - meta
properties:
  data:
    type: array
    items:
      type: object
      required:
        - id
        - type
        - attributes
      properties:
        id:
          type: string
          example: 855912
        type:
          type: string
          example: "al_triage_teams"
        attributes:
          type: object
          required:
            - triageTeamId
            - name
            - stationNumber
            - blockedStatus
            - preferredTeam
            - relationType
            - locationName
            - leadProviderName
            - teamName
            - suggestedNameDisplay
            - healthCareSystemName
            - groupTypeEnumVal
            - subGroupTypeEnumVal
            - groupTypePatientDisplay
            - subGroupTypePatientDisplay
            - ohTriageGroup
            - migratingToOh
          properties:
            triageTeamId:
              type: integer
              example: 855912
            name:
              type: string
              example: RADIOLOGY_TRIAGE_GROUP_1
            stationNumber:
              type: string
              example: 989
            blockedStatus:
              type: boolean
              description:
                Indicates if messaging to this team is blocked. Can be blocked for two reasons -
                (1) patient was blocked by coordinators from messaging certain teams, or
                (2) blocked due to migration to Oracle Health in certain phases.
              example: false
            preferredTeam:
              type: boolean
              example: true
            relationType:
              type: string
              enum: [PATIENT]
              example: PATIENT
            locationName:
              type: string
              example: "Main Hospital"
            leadProviderName:
              type: string
              example: "Dr. Smith"
            teamName:
              type: string
              example: "Radiology Team"
            suggestedNameDisplay:
              type: string
              example: "Radiology Triage Group 1"
            healthCareSystemName:
              type: string
              example: "VA Health Care System"
            groupTypeEnumVal:
              type: string
              example: "Primary"
            subGroupTypeEnumVal:
              type: string
              example: "Secondary"
            groupTypePatientDisplay:
              type: string
              example: "Primary Care"
            subGroupTypePatientDisplay:
              type: string
              example: "Specialty Care"
            ohTriageGroup:
              type: boolean
              example: false
            migratingToOh:
              type: boolean
              description: Indicates if this triage team's facility is migrating to Oracle Health
              example: false
  meta:
    type: object
    required:
      - sort
      - associatedTriageGroups
      - associatedBlockedTriageGroups
    properties:
      sort:
        type: object
        required:
          - name
        properties:
          name:
            type: string
            enum: [ASC, DESC]
            example: ASC
      associatedTriageGroups:
        type: integer
        example: 38
      associatedBlockedTriageGroups:
        type: integer
        example: 1
      friendlyTriageTeamPilotFacilities:
        type: string
        example: "979,989"
