services:
  mongodb:
    image: mongo:5.0
    container_name: mongodb-audit-db
    environment:
      MONGO_INITDB_DATABASE: mongodb-audit-db
      MONGO_URI: mongodb://localhost:27017/mongodb-audit-db
    ports:
      - 27017:27017
    volumes:
      - ./data:/data/db