# frozen_string_literal: true

require 'rails_helper'

RSpec.describe V0::Profile::GrantsController, type: :controller do
  context 'when user is logged in' do
    pending 'returns the grants from okta'  
  end
end