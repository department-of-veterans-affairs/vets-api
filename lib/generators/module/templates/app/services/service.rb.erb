# frozen_string_literal: true

module <%= class_name %>
  class Service < Common::Client::Base
    configuration <%= class_name %>::Configuration

    def get_message
      response = perform(:get, '/', {})
      Resource.new(response.body.symbolize_keys)
    end
  end
end
