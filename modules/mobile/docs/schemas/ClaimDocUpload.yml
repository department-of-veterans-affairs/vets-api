type: object
additionalProperties: false
required:
  - data
properties:
  data:
    type: object
    additionalProperties: false
    required:
      - jobId
    properties:
      jobId:
        type: string
        enum:
          - success
          - failure
        example: 'success'
        description: job status

