type: object
required:
  - city
  - state
properties:
  city:
    maxLength: 64
    type: string
    example: 'Helena'
  state:
    maxLength: 32
    type: string
    example: 'MT'
description: Required for CC Requests. The location that the veteran requested the appointment to be scheduled in.